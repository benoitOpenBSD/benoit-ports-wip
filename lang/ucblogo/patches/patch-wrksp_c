$OpenBSD: patch-wrksp_c,v 1.1.1.1 2006/06/01 05:58:15 jolan Exp $
--- wrksp.c.orig	Sat Aug 23 21:51:14 2008
+++ wrksp.c	Sat Mar 21 10:56:16 2009
@@ -27,6 +27,10 @@
 #include <windows.h>
 #endif
 
+#include <sys/types.h>
+#include <sys/wait.h>
+#include <ctype.h>
+
 #define WANT_EVAL_REGS 1
 #include "logo.h"
 #include "globals.h"
@@ -1576,7 +1580,7 @@ NODE *ledit(NODE *args) {
 #endif
 #else	/* !ibm (so unix) */
     if (fork() == 0) {
-	execlp(editor, editorname, tmp_filename, 0);
+	execlp(editor, editorname, tmp_filename, (void*)NULL);
 	exit(1);
     }
     wait(0);
