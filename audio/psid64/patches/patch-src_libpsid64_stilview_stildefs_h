Index: src/libpsid64/stilview/stildefs.h
--- src/libpsid64/stilview/stildefs.h.orig
+++ src/libpsid64/stilview/stildefs.h
@@ -5,7 +5,7 @@
 #ifndef _STILDEFS_H
 #define _STILDEFS_H
 
-#if defined(__linux__) || defined(__FreeBSD__) || defined(solaris2) || defined(sun) || defined(sparc) || defined(sgi) || defined(__APPLE__)
+#if defined(__linux__) || defined(__FreeBSD__) || defined(solaris2) || defined(sun) || defined(sparc) || defined(sgi) || defined(__APPLE__) || defined (__OpenBSD__)
 #define UNIX
 #endif
 
