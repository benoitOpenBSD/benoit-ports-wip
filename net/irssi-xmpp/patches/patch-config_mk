$OpenBSD$
--- config.mk.orig	Wed Feb 15 10:45:37 2012
+++ config.mk	Mon Apr 23 10:50:23 2012
@@ -2,13 +2,13 @@
 PREFIX ?= /usr/local
 
 # where are the sources of irssi?
-IRSSI_INCLUDE ?= ${PREFIX}/include/irssi
+IRSSI_INCLUDE ?= ${TRUEPREFIX}/include/irssi
 # where should be installed the module?
-IRSSI_LIB ?= ${PREFIX}/lib/irssi
+IRSSI_LIB ?= ${TRUEPREFIX}/lib/irssi
 # where should be installed the documentation?
-IRSSI_DOC ?= ${PREFIX}/share/doc/irssi
+IRSSI_DOC ?= ${TRUEPREFIX}/share/doc/irssi
 # where should be installed the help for commands ?
-IRSSI_HELP ?= ${PREFIX}/share/irssi/help
+IRSSI_HELP ?= ${TRUEPREFIX}/share/irssi/help
 
 # includes and libs
 INCS =	${LIB_INCS} \
@@ -22,11 +22,11 @@ INCS =	${LIB_INCS} \
 LIBS =	${LIB_LIBS}
 
 # flags
-CFLAGS = -fPIC -std=c99 -DUOFF_T_LONG
+CFLAGS += -fPIC -DUOFF_T_LONG
 LDFLAGS = -shared
 
 # debug
 #CFLAGS += -W -g -Wall -Wno-unused-parameter
 
 # compiler and linker
-CC = cc
+CC ?= cc
