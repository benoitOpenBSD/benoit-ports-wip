--- src/capture/capctl.c.orig	Sun Jun  3 20:40:44 2018
+++ src/capture/capctl.c	Fri Jan 17 13:42:25 2020
@@ -316,7 +316,7 @@
 }
 
 /* FIXME: verify how this works w/ negatives... */
-static inline gint64 tv_to_ms(const struct timeval *tv)
+static inline gint64 tv_to_ms(const struct bpf_timeval *tv)
 {
   gint64 ms = tv->tv_sec * 1000;
   if (ms > 0)
@@ -333,7 +333,7 @@
   int status;
   struct pcap_pkthdr *nextpkt_hdr;
   const unsigned char *nextpkt_data;
-  struct timeval pktdelta;
+  struct bpf_timeval pktdelta;
   size_t copylen;
 
   if (capture_status != PLAY)
