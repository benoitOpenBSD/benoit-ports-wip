--- src/capture/capctl.c.orig	Sun Jun  3 20:40:44 2018
+++ src/capture/capctl.c	Fri Jan 17 14:21:25 2020
@@ -37,6 +37,13 @@
 #include <sys/wait.h>
 #endif
 
+#if defined(__OpenBSD__)
+#include <net/bpf.h>
+typedef struct bpf_timeval pkt_timeval;
+#else
+typedef struct timeval pkt_timeval;
+#endif
+
 #include <glib.h>
 #include <pcap.h>
 
@@ -333,7 +340,7 @@
   int status;
   struct pcap_pkthdr *nextpkt_hdr;
   const unsigned char *nextpkt_data;
-  struct timeval pktdelta;
+  pkt_timeval pktdelta;
   size_t copylen;
 
   if (capture_status != PLAY)
