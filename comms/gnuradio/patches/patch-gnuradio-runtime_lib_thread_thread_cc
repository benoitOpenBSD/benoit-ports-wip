--- gnuradio-runtime/lib/thread/thread.cc.orig.port	Thu Feb 20 02:23:56 2025
+++ gnuradio-runtime/lib/thread/thread.cc	Sun Jan 18 08:08:35 2026
@@ -259,6 +259,8 @@
     pthread_setname_np(name.c_str());
 #elif defined(__NetBSD__)
     pthread_setname_np(thread, name.c_str(), nullptr);
+#elif defined(__OpenBSD__)
+    pthread_set_name_np(thread, name.c_str());
 #else
     thread_logger().error("function {}{} not implemented on this BSD-alike platform",
                           __func__,
