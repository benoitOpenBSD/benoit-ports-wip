--- src/effects/smoke.c.orig	Wed Jan 24 02:43:50 2007
+++ src/effects/smoke.c	Tue May 19 22:55:46 2009
@@ -24,8 +24,10 @@
 #include <stdlib.h>
 #include <string.h>
 
+#ifndef MIN
 #define MIN(x, y)    \
     (((x) < (y)) ? (x) : (y))
+#endif
 
 #define ABS(x)      \
     (((x) >= 0) ? (x) : ( - (x)))
